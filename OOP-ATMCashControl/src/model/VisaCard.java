package model;

public class PrepaidCard extends Card{
    public PrepaidCard(String id, String userName, String passWord, String firstName, String lastName, double accountBalance) {
        super(id, userName, passWord, firstName, lastName, accountBalance);
    }
}
